# Image Generation

1. Start virtual environment and make sure all dependencies are installed.
1. Make sure that you create a `.env` file in `api` with an API key for OpenAI.
1. Run `api/main.py` or start debugging. Wait for the `Application startup complete`.
1. Open a different terminal and run `app/main.py`. 
1. Look at the results.